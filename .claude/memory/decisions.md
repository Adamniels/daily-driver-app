# Decisions

Why we chose X over Y, so it isn't re-litigated later. Newest first.

<!-- Example:
## 2026-06-12 — Memory lives in the repo, not the harness memory dir
**Decision:** Keep durable project memory in committed `.claude/memory/` files.
**Why:** The built-in `~/.claude/.../memory/` dir is machine-local and doesn't sync
across computers; committed files travel via git.
**Alternatives considered:** Synced folder (Dropbox/iCloud) — more friction, only
worth it if the notes must stay private.
-->
